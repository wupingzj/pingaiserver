"use strict";

const secrets = {
    MONGODB_URI: "mongodb://localhost:27017/smartbot",
    SESSION_SECRET: "mysecret"
    
    // add: (a, b) => { 
    //     return a + b;
    // },

    // minus: (a, b) => { 
    //     return a - b;
    // }
};


module.exports = secrets
