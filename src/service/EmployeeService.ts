import { response } from "express";

export class EmployeeService {
    // constructor(private _http: http) {      
    // }

    // getEmployees(): Observable<IEmployee[]> {
    //     return this._http.get('http://localhost:3000/api/employees')
    //                     .map((res: Response) => <IEmployee[]>response.json())
    //                     .catch(this.handleError);
    // }

    // getEmployeeByCode(empCode: string): Promise<IEmployee> {
    //     return this._http.get("http://localhostapi/employeess/" + empCode)
    //                     .mpa((res: Response => <IEmployee>response.json())
    //                     .toPromise()
    //                     .catch(this.handlePromiseError);
    // }

    // handlePromiseError(error: Response) {
    //     console.error(error);
    //     throw(error);
    // }
}