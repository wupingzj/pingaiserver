"use strict";

// const myadd = {
//     add: function(a, b) { 
//         return a + b;
//     }    
// };

const myutil = {
    add: (a, b) => { 
        return a + b;
    },

    minus: (a, b) => { 
        return a - b;
    }
};


module.exports = myutil
